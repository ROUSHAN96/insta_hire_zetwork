# Environment Variables Template
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PostgreSQL Database URL (Neon DB or Local PostgreSQL)
# Example for Neon Serverless PostgreSQL:
# DATABASE_URL="postgresql://[user]:[password]@[endpoint].neon.tech/[dbname]?sslmode=require"
# Example for Local PostgreSQL (via docker compose):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/insta_hire_zetwork?schema=public"
